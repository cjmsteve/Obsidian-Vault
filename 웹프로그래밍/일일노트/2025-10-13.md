기본 자료형과 객체 자료형의 차이
number 객체 
let Fixed = num.toFixed(2);  //소수점
let Exact = num.toExponential(2); 
let Precise = num.toPrecision(2);

과도하게 큰수를 다룰때는 작은수 무시
Number.MAX_VALUE -> 최대수

string.toUpperCase();

Date();
Array 객체
-split();